# `@benjamin/eslint-config`

> My personal [ESLint](https://eslint.org) config.

## Usage

**Install**:

```bash
$ yarn add --dev @benjamin/eslint-config
```

**Edit `.eslintrc`**:

```jsonc
{
  "extends": "@benjamin/eslint-config"
}
```
